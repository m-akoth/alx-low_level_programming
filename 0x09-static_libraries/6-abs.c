#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @n: Integer to be checked
 * Return: Absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
